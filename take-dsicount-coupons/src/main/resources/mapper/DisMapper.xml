<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mr.takedsicountcoupons.mapper.DisMapper">
    <select id="queryTotal" parameterType="com.mr.take.domain.Discount" resultType="int">
        select count(*) from t_discount
    </select>

    <select id="queryCoupon" parameterType="com.mr.take.domain.Discount" resultType="com.mr.take.domain.Discount">
        select * from t_discount
    </select>
</mapper>